mbcproject.py contains the script used to format data, build model, train model and convert to onnx form.
score.py contains script used to score model when deployed on Microsoft Azure container instance.
